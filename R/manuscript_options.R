## All options for building the manuscript go here
manuscript_options <- function(data_output = "data-output",
                               figures = "fig",
                               year = 1980) {
    list(data_output = data_output,
         figures = figures,
         year = year)
}
